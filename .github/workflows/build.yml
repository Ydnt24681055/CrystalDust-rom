name: Build ROM

on:
  push:
    branches:
      - progress

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkitarm:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure and Initialize Submodules
        run: |
          git config --global --add safe.directory /__w/CrystalDust-rom/CrystalDust-rom
          # This command forces the submodules to actually download their files
          git submodule sync --recursive
          git submodule update --init --recursive --force

      - name: Verify tools content
        run: |
          echo "Listing contents of tools/ directory:"
          ls -R tools/

      - name: Build tools
        run: |
          # Try to build the tools after verifying they are present
          make -C tools

      - name: Build ROM
        run: |
          make clean
          make -j$(nproc)

      - name: Upload ROM
        uses: actions/upload-artifact@v4
        with:
          name: crystal-dust-rom
          path: '*.gba'